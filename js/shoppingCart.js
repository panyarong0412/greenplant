window.onload=function(){
       let product=;
       let counts=$("");
       let classprise=0;
	   $oneprise=$("#oneprise").html();
	   $count=$("#count").val();
	   $nomalprise=$("#nomalprise").html();
	   // console.log($oneprise);
	   console.log($count);
	 for(let i=0;i<$count.length;i++){

	 }
	   $nomalprise=$count*$oneprise;
	    console.log($nomalprise)
	}